<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
      Mini Raccourcisseur d'URL + QR
    </title>
    <script src="https://cdn.tailwindcss.com">

    </script>
  </head>
  <body class="min-h-screen bg-slate-50">
    <div class="max-w-2xl mx-auto px-4 py-10">
      <h1 class="text-2xl font-bold text-slate-900">
        Mini Raccourcisseur d'URL + Génération de QR Code
      </h1>
      <p class="mt-2 text-slate-600">
        Colle une URL longue, on te renvoie une URL courte + un QR code.
      </p>

      <form method="POST" action="/shorten" class="mt-6 bg-white rounded-lg border p-4">
        {{ csrfField() }}
        <div class="flex flex-col gap-2">
          <label for="originalUrl" class="text-sm font-medium text-slate-700">URL originale</label>
          <input
            id="originalUrl"
            name="originalUrl"
            type="text"
            placeholder="https://exemple.com/une-url-tres-longue"
            class="w-full rounded-md border px-3 py-2"
            required
          />

          @if(errors)
            <div class="mt-2 rounded-md border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-700">
              @each(error in errors)
                <div>
                  {{ error.message }}
                </div>
              @endeach
            </div>
          @endif

          <button
            type="submit"
            class="mt-3 inline-flex items-center justify-center rounded-md bg-indigo-600 px-4 py-2 text-white"
          >
            Raccourcir
          </button>
        </div>
      </form>

      @if(result)
        <div class="mt-6 bg-white rounded-lg border p-4">
          <h2 class="text-lg font-semibold text-slate-900">
            Résultat
          </h2>

          <div class="mt-3">
            <div class="text-sm text-slate-500">
              URL courte
            </div>
            <a class="text-indigo-600 underline break-words" href="{{ result.shortUrl }}" target="_blank">{{ result.shortUrl }}</a>
          </div>

          <div class="mt-3">
            <div class="text-sm text-slate-500">
              QR Code
            </div>
            <img src="{{ result.qrCodeDataUrl }}" alt="QR Code" class="mt-2 h-44 w-44" />
          </div>

          <div class="mt-3">
            <div class="text-sm text-slate-500">
              URL originale
            </div>
            <div class="break-words text-slate-800">
              {{ result.originalUrl }}
            </div>
          </div>
        </div>
      @endif
    </div>
  </body>
</html>
